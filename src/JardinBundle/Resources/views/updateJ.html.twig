{% extends 'layout.html.twig' %}

{% block content %}
    <center><H1>UPDATE JArdin</H1></center>
    {% for jardin in jardins %}

    <div class="card mb-3" style="width: 60% ;left: 20%" >
        <div class="card-header form-inline ">
            <img alt=""  src="{{ asset(jardin.logo) }}"  class="col-4">

            <h1 class="col-4">{{ jardin.nom}}</h1>

            <img alt=""  src="{{ asset('images/images.png') }}"  class="col-4"></div>

            <br>
        <div class="card-body">

                  body
        </div>
        <div class="card-footer">

            <a href="#" class="btn btn-flat btn-colored btn-theme-color-lemon mt-15 mr-15">Mettre a jour</a>
            <a href="{{ path('deleteJ',{'id':jardin.id}) }}" class="btn btn-flat btn-colored btn-theme-color-red mt-15">Supprimer</a>
        </div>

    </div>
    {% endfor %}


{% endblock %}
